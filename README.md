# Cloud Infrastructure Provisioner

## Project Overview
A CLI tool designed to validate cloud-native infrastructure plans. Built to demonstrate proficiency in Cloud Strategy, VPC architecture, and automated validation.

## Setup Instructions
1. Clone the repo: `git clone https://github.com/YOUR_USERNAME/REPO_NAME.git`
2. Navigate to folder: `cd REPO_NAME`
3. (Optional) Run `pip install -r requirements.txt`

4. ## Open in GitHub Codespaces

For the fastest evaluation, you can run this project directly in your browser using GitHub Codespaces. This ensures a pre-configured environment that is 100% compatible with the project requirements.

1. **Click the "Code" button** at the top right of this repository.
2. **Select the "Codespaces" tab**.
3. **Click on "Create codespace on main"**.
4. Once the terminal initializes, run the execution command directly:
   ```bash
   python src/main.py templates/infrastructure.json


